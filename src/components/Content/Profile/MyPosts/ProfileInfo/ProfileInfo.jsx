import React from 'react';
import modClass from './ProfileInfo.module.css'

function ProfileInfo(props) {
    return (
        <div className={modClass.wrapper}>
            ava + description
        </div>
    );
}

export default ProfileInfo;